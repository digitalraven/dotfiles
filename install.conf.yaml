- defaults:
    link:
      create: true
      relink: true

- clean: ["~"]

- shell:
    - brew bundle
    - sh pre-install.sh

- link:
    ~/.gitconfig: gitconfig
    ~/.vim: vim/
    ~/.vimrc: vimrc
    ~/.bashrc: bashrc
    ~/.bash_profile: bash_profile
    ~/.zshrc:
        force: true
        path: zshrc
    ~/.oh-my-zsh/custom/themes/digitalraven.zsh-theme:
        path: digitalraven.zsh-theme
